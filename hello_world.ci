# This is a basic workflow file to show off some of the features in Cicada

on git.push

echo Hello from (event.provider)!
echo

shell neofetch

echo Contents of this workflow file:

let filename = "hello_world.ci"

shell cat (filename)
